<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:background="@android:color/white">

    <!--
        Icon — backend tooltip target: "bs_item_icon"
        This is the view Joel's team would target. It gets recycled
        as the user scrolls, which is exactly what triggers Bug #4.
    -->
    <ImageView
        android:id="@+id/bs_item_icon"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:scaleType="centerCrop"
        android:background="#EEF2FF"
        android:padding="8dp"
        android:src="@android:drawable/ic_menu_gallery"
        android:contentDescription="Product icon" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="8dp">

        <!-- Name — backend tooltip target: "bs_item_name" -->
        <TextView
            android:id="@+id/bs_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#1A1A1A"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/bs_item_category"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#999999"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Price — backend tooltip target: "bs_item_price" -->
    <TextView
        android:id="@+id/bs_item_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#0752AD"
        android:textSize="15sp"
        android:textStyle="bold" />

</LinearLayout>