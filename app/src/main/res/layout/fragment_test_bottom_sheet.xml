<?xml version="1.0" encoding="utf-8"?>
<com.appversal.appstorys.ui.xml.OverlayLayoutView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bs_overlay"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    app:insideBottomSheet="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="8dp"
        android:paddingEnd="24dp"
        android:paddingBottom="32dp">

        <!-- Drag handle -->
        <View
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="20dp"
            android:background="#CCCCCC" />

        <!-- Title — backend tooltip target: "xml_bs_title" -->
        <TextView
            android:id="@+id/xml_bs_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="12dp"
            android:text="XML Bottom Sheet"
            android:textColor="#0752AD"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Subtitle — backend tooltip target: "xml_bs_subtitle" -->
        <TextView
            android:id="@+id/xml_bs_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:gravity="center"
            android:text="This bottom sheet uses XML views with tooltip targeting."
            android:textColor="#888888"
            android:textSize="14sp" />

        <!--
            RecyclerView — each item has:
              android:id="@+id/bs_item_icon"   → backend tooltip target: "bs_item_icon"
              android:id="@+id/bs_item_name"   → backend tooltip target: "bs_item_name"
              android:id="@+id/bs_item_price"  → backend tooltip target: "bs_item_price"

            Scroll this list to verify:
              • Bug #3: tooltip coordinate updates on scroll
              • Bug #4: recycled views don't get highlighted instead of target
        -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/bs_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="260dp"
            android:layout_marginBottom="16dp"
            android:clipToPadding="false"
            android:paddingBottom="8dp" />

        <!-- Close button — backend tooltip target: "xml_bs_close_btn" -->
        <Button
            android:id="@+id/xml_bs_close_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="#0752AD"
            android:text="Close" />

    </LinearLayout>

</com.appversal.appstorys.ui.xml.OverlayLayoutView>